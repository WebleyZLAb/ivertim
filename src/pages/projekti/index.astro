---
import { getCollection } from "astro:content";
import PageLayout from "../../layouts/PageLayout.astro";

const projekti = (await getCollection("projekti")).sort(
  (a, b) => (a.data.order ?? 99) - (b.data.order ?? 99)
);
---

<PageLayout
  title="Projekti"
  description="Realizovani projekti Ivertim — problem, proces i rješenje za svaki prostor."
>
  <h1>Projekti</h1>
  <p class="lead">
    Ne prikazujemo namještaj. Prikazujemo riješene prostore.
  </p>
  {
    projekti.length === 0 ? (
      <p class="empty">Prvi projekti stižu uskoro.</p>
    ) : (
      <ul class="project-list">
        {projekti.map((projekat) => (
          <li>
            <a href={`/projekti/${projekat.id}`}>{projekat.data.title}</a>
            <p>{projekat.data.description}</p>
          </li>
        ))}
      </ul>
    )
  }
</PageLayout>

<style>
  h1 {
    font-size: var(--text-h1-size-fluid);
    line-height: var(--text-h1-leading);
    font-weight: var(--text-h1-weight);
    max-width: var(--content-max);
  }

  .lead {
    margin-top: var(--space-lg);
    max-width: var(--content-max);
    font-size: var(--text-body-lg-size);
    line-height: var(--text-body-lg-leading);
    color: var(--text-secondary);
  }

  .empty {
    margin-top: var(--space-xl);
    color: var(--text-tertiary);
  }

  .project-list {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .project-list li {
    padding: var(--space-lg);
    border: var(--border-1) solid var(--border-subtle);
    border-radius: var(--radius-3);
  }
</style>
