---
import { getCollection } from "astro:content";
import PageLayout from "../../layouts/PageLayout.astro";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf()
);
---

<PageLayout
  title="Blog / Savjeti"
  description="Savjeti o namještaju po mjeri, planiranju prostora i procesu proizvodnje."
>
  <h1>Blog / Savjeti</h1>
  <p class="lead">
    Stranica Savjeti postoji da pomogne ljudima prije nego što donesu odluku.
  </p>
  {
    posts.length === 0 ? (
      <p class="empty">Prvi članci stižu uskoro.</p>
    ) : (
      <ul class="post-list">
        {posts.map((post) => (
          <li>
            <a href={`/blog/${post.id}`}>{post.data.title}</a>
            <p>{post.data.description}</p>
          </li>
        ))}
      </ul>
    )
  }
</PageLayout>

<style>
  h1 {
    font-size: var(--text-h1-size-fluid);
    line-height: var(--text-h1-leading);
    font-weight: var(--text-h1-weight);
    max-width: var(--content-max);
  }

  .lead {
    margin-top: var(--space-lg);
    max-width: var(--content-max);
    font-size: var(--text-body-lg-size);
    line-height: var(--text-body-lg-leading);
    color: var(--text-secondary);
  }

  .empty {
    margin-top: var(--space-xl);
    color: var(--text-tertiary);
  }

  .post-list {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .post-list li {
    padding: var(--space-lg);
    border: var(--border-1) solid var(--border-subtle);
    border-radius: var(--radius-3);
  }
</style>
