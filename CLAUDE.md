# CLAUDE.md

This file is Claude Code's working memory for the Ivertim project. It is **not** a source of strategic truth — `README.md`, `/docs` and `/design-system` always take precedence. This file only tracks status, decisions, and open items between sessions.

Authority hierarchy (per `README.md`): `README.md` → `/docs` → `/design-system` → explicit new owner decision → existing code → AI/developer assumptions. If something is undefined: don't invent a value, don't introduce a new pattern, propose and explain, wait for approval.

**Workflow rule:** all strategy/branding/design-system documentation was produced with ChatGPT before this handoff. Claude Code now owns the Development phase. Working mode is always **plan first, build second** — nothing gets implemented without an explicit go-ahead in that session. At the end of every meaningful session, this file's Current Progress / Completed / Current Task / Next Task / Session Notes sections must be updated so the next session can pick up exactly where this one left off.

**Copywriting & Marketing:** the owner continues to write copy and run marketing with ChatGPT, not here. Claude Code keeps read/reference visibility into `docs/01-branding.md` and `docs/08-marketing.md` (voice, tone, banned/preferred phrasing, content mix) so implementation stays consistent with whatever copy arrives, and should flag — not silently accept — any incoming copy that contradicts the locked voice/tone rules. **Update 2026-09-02: the full copywriting blueprint set already exists in-repo** at `docs/04 - Copywriting/` (Communication System + per-page blueprints, all marked "Approved") — it was added directly on GitHub 2026-07-22/23, outside a Claude Code session, so earlier notes in this file describing it as "not yet delivered" were stale. **Update 2026-09-03: the set's revision against the service pivot is now done** (see Session Notes) — the last gap was `00 - Communication System.md` itself (the tone doc every other file defers to), which was still furniture-only; it and `Process-Blueprint` are now v1.1 too. **Update 2026-09-03 (later): the planned skill is also built** — `.claude/skills/ivertim-copywriting/SKILL.md`, see Next Task item 1.

**✅ HOLD LIFTED 2026-09-03.** Set 2026-07-11, reconfirmed 2026-07-12, held while Copywriting and the visual-identity brandbook were outstanding. Both were done earlier this same session (Copywriting revision + `ivertim-copywriting` skill; brandbook built with `/design`, reviewed and published). Owner then explicitly confirmed via a direct question ("Da li dižemo dev hold i krećemo sa pravim sadržajem/stranicama?") → **"Da, digni hold — krenimo."** Real page/component/content work may now proceed. Kept here for history — this paragraph should not be read as still-active guidance.

**What "lifted" does and doesn't cover:** build real pages/components/content using the locked docs, `design-system` tokens, the brandbook, and the `ivertim-copywriting` skill. Still respect the authority hierarchy for anything genuinely undefined (propose, don't invent). **Projekti** has no real material yet (needs real case studies/photos the owner hasn't provided) and should stay structurally ready but empty — don't fabricate placeholder projects to fill it, flag it instead when its turn comes. **Blog/Savjeti** is no longer in that category as of 2026-09-04: the owner explicitly asked for real articles (not just structure) after confirming there are no photo-dependent tasks left to do, and educational how-to content doesn't carry the same fabrication risk as invented case studies of real completed work — see Session Notes for the reasoning and what was published.

## Project Summary

**Updated 2026-09-02 — see Session Notes for the full pivot.** Ivertim is a Prijedor-based company now positioned primarily as a board-cutting service center ("drvni centar"): (1) service cutting, edge-banding and drilling of board materials — primary, capacity-first — and (2) made-to-measure panel furniture (kitchens, wardrobes, cabinets, etc.) — secondary, still fully offered but subordinate to cutting-service capacity. Full apartment/house fit-outs still happen but are not a separately marketed service (saturated referral network). 3D design, CNC production, transport and installation remain part of the delivery *process* for both services, not separate line items.

Brand essence: **"Kompetentnost kojoj vjeruješ"** (Competence you can trust). Tone: calm, precise, advisory, never salesy or superlative-driven. Verbal principle: *Slušaj → postavi → shvati → izmjeri → predloži* (Listen → ask → understand → measure → propose).

Primary audience: new apartment/house owners, buyers wanting full fit-outs, small carpentry workshops needing service-cutting. Architects are explicitly not the primary target in phase one.

## Current Architecture

**Astro + TypeScript**, static-first (`output: "static"` + `@astrojs/vercel` adapter), approved by owner + ChatGPT on 2026-07-11. Only two routes opt out of prerendering (`export const prerender = false`): `/kontakt` (needs to read post-submit `?status=` query params) and `/api/contact` (POST handler). Everything else is static HTML at build time.

- **Content:** Astro Content Collections, markdown/MDX in-repo (`src/content/usluge`, `src/content/projekti`, `src/content/blog`), schemas in `src/content.config.ts`. No CMS, no database. `usluge` has real content; `blog` has 3 real articles (2026-09-04, see below); `projekti` remains empty — waiting on real case studies/photos.
- **Blog/Savjeti structure (built 2026-09-04) + 3 real articles (added same day, see Session Notes):** `blog` schema adds `topic` (reader-facing category, six-value enum from `Advice-Blueprint-v1.0.md` §11 — distinct from the existing `category` field, which is the internal edukativno/prodajno/ostalo 50/30/20 tracking field from `docs/08-marketing.md` and isn't shown to readers) and `featured` (boolean, picks the landing page's "Istaknuti savjeti" cards, falls back to most recent). `src/lib/blog.ts` centralizes the topic label map and a reading-time estimate computed from each entry's raw body (~200 wpm) — no manual "vrijeme čitanja" field to keep in sync. `src/pages/blog/index.astro` and `[slug].astro` follow `Advice-Blueprint-v1.0.md` / `Advice-Template-Blueprint-v1.0.md` structure and copy verbatim where final copy exists; the article template's closing CTA is fixed, identical copy hardcoded once in the template (per the blueprint), not authored per post. Deliberately left out for now (YAGNI until real articles exist): category filtering (categories render as a static list) and an archive/pagination view beyond the 4 featured cards — flagged as a real gap once post count exceeds 4, not built preemptively.
- **Styling/tokens:** `src/styles/tokens.css` — CSS custom properties generated 1:1 from `/design-system/*.md` (colors, spacing, geometry, motion, layout, typography scale, icon scale, z-index). `src/styles/global.css` is the reset + base application of those tokens. No Tailwind.
- **Typography:** self-hosted via `@fontsource-variable/instrument-sans` (avoids third-party font requests to Google Fonts), loaded in `src/layouts/BaseLayout.astro`.
- **Fluid type scale:** display/H1–H3 use `clamp()` with mobile minimums Claude Code chose (docs left exact formulas to implementation) — see file header comment in `tokens.css`. Flagged in Known Issues for visual validation, not a locked design decision.
- **Layout:** `src/layouts/BaseLayout.astro` (HTML shell + font + meta) → `src/layouts/PageLayout.astro` (Header + main + Footer wrapper, reused by all simple pages).
- **Routing:** file-based, matches locked IA — `/`, `/usluge` + `/usluge/[slug]`, `/projekti` + `/projekti/[slug]`, `/proces-saradnje`, `/blog` + `/blog/[slug]`, `/kontakt`.
- **Contact form:** `src/pages/kontakt.astro` (plain HTML `<form method="POST" action="/api/contact">`, works without JS) → `src/pages/api/contact.ts` (server-side validation, honeypot field, redirects with `?status=`) → `src/lib/email.ts` (Resend, isolated/swappable, reads `RESEND_API_KEY` / `CONTACT_EMAIL_TO` / `CONTACT_EMAIL_FROM` from env — none set yet, see `.env.example`). Astro's built-in same-origin POST protection is active by default (verified).
- **Repo:** git initialized locally 2026-07-11; pushed to GitHub 2026-07-12 at [github.com/WebleyZLAb/ivertim](https://github.com/WebleyZLAb/ivertim) (owner-provided remote, owner-created repo).
- **SEO/technical housekeeping (added 2026-09-04):** `astro.config.mjs` now sets `site: "https://ivertim.vercel.app"` (Vercel's stable project alias — found via the Vercel MCP tools since no custom domain is connected yet; **update this the day a custom domain goes live**) and adds the `@astrojs/sitemap` integration (auto-generates `sitemap-index.xml`/`sitemap-0.xml` from every rendered route, `kontakt` included). `public/robots.txt` allows everything and points at the sitemap. `src/pages/404.astro` is a real on-brand 404 page (new microcopy, not from any blueprint — none covers this). `BaseLayout.astro` now emits a canonical link plus basic Open Graph/Twitter meta tags per page (title/description reused, no `og:image` since there's no image asset suited for one yet — deliberately left out rather than reusing an unsuited logo file).
- **Logo assets:** **Updated 2026-09-03 — final SVG arrived from owner**, superseding most of the PNG placeholder set: `public/images/logo-horizontal.svg` / `-inverse.svg` (horizontal lockup, light/dark), `logo-mark.svg` / `-inverse.svg` (standalone mark), `logo-wordmark.svg` / `-inverse.svg` (standalone wordmark, new — didn't exist as a separate asset before), `signature-shape.svg` / `-inverse.svg` (vector source for the Signature System shape, described in `docs/09 - Signature System.md` but never had a real vector asset until now). Old `logo-horizontal.png` and `logo-mark.png` were removed 2026-09-03 (owner confirmed). `logo-vertical.png` remains the only vertical lockup — no SVG delivered for it yet. Still missing per `docs/02-logo-identity.md`'s variant list: vertical lockup SVG, favicon, app/social icon, monochrome-only variants. Now wired into `Header.astro`/`Footer.astro`; `public/favicon.svg` uses `logo-mark.svg` (owner confirmed this is fine as an interim favicon).

## Current Progress

- **Phase:** Real content phase, started 2026-09-03. Home, Usluge (landing + 2 service pages), Proces saradnje, Kontakt, and now Blog/Savjeti (structure + 3 real articles, 2026-09-04) have real content, not placeholders — see Next Task items 2/3/5/6 (done) and Session Notes. Projekti remains structurally ready but empty (no real case studies/photos from the owner yet) — the only content type still waiting.
- **Design System:** v1.0.0, locked for this website phase, and now fully mirrored into `src/styles/tokens.css`.

## Completed

- Full documentation read-through: `README.md`, `PROJECT_RULES.md`, `CHANGELOG.md`, `CLAUDE_FIRST_PROMPT.md`, `REPOSITORY_STRUCTURE.md`, all 8 files in `/docs`, all 12 files in `/design-system`.
- Created this `CLAUDE.md` as operational memory, per `CLAUDE_FIRST_PROMPT.md` and `design-system/ai-behaviour.md`.
- Locked Instrument Sans Variable as the official typeface. Updated `design-system/typography.md` (Font Family section replacing the placeholder), `README.md` (Design System section), and `CHANGELOG.md` (Locked list) per explicit owner instruction.
- Stack proposal (Astro + TS) discussed, refined with ChatGPT's input, and approved by owner.
- Scaffolded the full Astro project: token CSS from `/design-system`, base/page layouts, Header/Footer, all IA routes, content collection schemas, contact form + server endpoint + isolated email module, `.env.example`. Verified `npm run dev` and `npm run build` both succeed; manually tested the contact endpoint (valid submission, honeypot, missing fields, invalid email) via direct HTTP requests — all behaved correctly. Initialized git and made the first local commit.

## Current Task

**Session ended 2026-09-04 by owner request — read this whole section first before doing anything.** Dev hold lifted 2026-09-03; real page/component/content work is active and this first pass (Home, Usluge landing + 2 entries, Proces saradnje, Kontakt, mobile nav, real logo, motion, Signature Shape accents) is done and deployed. Owner's read on it, verbatim in spirit: **"opet ću samo prokomentarisati... nije to to"** — decent structure, but doesn't yet feel like a real, visually striking site. Two explicit reasons: (1) no real photography yet (expected, not a bug — see prior Session Notes), (2) **visual design itself needs another iteration pass that hasn't happened yet.** Owner said explicitly: going forward they will hand over **their own ideas plus screenshots of inspiration from other sites/things they've seen** for specific visual outcomes they want, alongside real photos when ready — that's how this gets to where they want it, not by me polishing alone against the docs. Do not treat "start of next session" as "resume building the same direction" — wait for that input first if it hasn't arrived yet.

**Owner's stated visual bar** (record this precisely, it's a standard to hold future work to): the site must be **readable AND visually striking** — the impression should be not just "a serious/competent company" but **"a company that invests in visual/aesthetic feel"** — a higher bar than what design-system compliance alone produces. This isn't a request to break locked tokens/rules; it's a signal that correct-but-safe execution of the existing system isn't sufficient on its own — craft and composition matter, not just correctness.

**Signature Shape specifically flagged as wrong right now**: owner said it needs to be used "na pravim mjestima i u drugačijoj poziciji" (in the right places, and in a different position) than the current placements (Home hero background corner, one card corner, mobile menu icon) — **no specifics given yet, explicitly deferred to next session** ("da sad ne dužim"). Don't guess at a fix — ask what they mean first.

~~**Concrete, unambiguous gap found this session: FAQ has no real answers anywhere except Kontakt.**~~ — **done 2026-09-04, next session.** Both files now have real answers, written via the `ivertim-copywriting` skill, deliberately generic wherever a real fact (exact material/thickness list, minimum order quantity, lead time) is still unconfirmed by the owner — consistent with `Cutting-Service-Blueprint-v1.0.md` §15 TODO, which stays open. `usluzno-rezanje-kantovanje-busenje.md`'s 5 questions and `izrada-namjestaja-po-mjeri.md`'s 3 questions were converted from bare bullet lists to `### question` + answer paragraph (matching the existing `### Kuhinje`-style pattern already used in the furniture file; no new CSS needed, `prose` styles already cover h3+p). Answers also backfilled into `Cutting-Service-Blueprint-v1.0.md` §12 (bumped to v1.1) so the blueprint stays the copy source of truth; the furniture-page questions are sourced from multiple mini blueprints (`Contact-`, `Custom-`, `Wardrobes-Blueprint`) with no single 1:1 file to update, so those were left as-is — the live page is the source of truth for that one. `npm run build` verified clean; page content spot-checked in the browser preview via `get_page_text` (correct heading/paragraph order) — screenshot verification hit the same scroll-reveal/browser-tool flakiness already logged in Known Issues, not a real defect. Not yet committed.

**Longer-term roadmap the owner stated**: once the site itself is "done" (bar above, not yet met) → move to **Instagram content planning** as the next major phase. In parallel, owner will set up **Google Business Profile** themselves; once that exists, next session should help refine/optimize it (consistent NAP data with the site's LocalBusiness structured data, description, categories, etc. — nothing to prepare now, just tracked here so it isn't forgotten).

## Next Task

**Service-pivot documentation revision — done this session (2026-09-02), owner should spot-check before it's treated as final:**

1. ✅ `docs/00-strategy.md` §Poslovni fokus + `README.md` §2 — rewritten to the two-service list (rezanje/kantovanje/bušenje primary, namještaj po mjeri secondary), "kompletno opremanje stanova" dropped as a marketed line item, 3D/CNC/transport framed as process not services.
2. ✅ `docs/08-marketing.md` §Glavne teme — rebalanced toward rezanje/kantovanje/bušenje-for-radionice content first.
3. ✅ `docs/04 - Copywriting/Services-Landing-Blueprint-v1.0.md` — bumped to v1.1, restructured from "Za vaš dom" / "Za veće projekte" into the two real services; room categories moved to be examples/cards under "Izrada namještaja po mjeri" instead of separate services.
4. ✅ `docs/04 - Copywriting/Home-Blueprint-v1.0-Full.md` §USLUGE — H2/lead reworded to the two-service framing.
5. ✅ Decided (owner: "sav namještaj će se raditi... ti odluči kako je najbolje"): per-room mini blueprints (`Kitchens-`, `Wardrobes-`, `Living-Room-`, `Hospitality-`, `Business-`, `Custom-Blueprint-v1.0.md`) become examples/cards under "Izrada namještaja po mjeri" and/or Projekti filter categories — not standalone services with their own top-level URL. Each file got a 2026-09-02 note recording this; content itself untouched (still usable as-is).
6. ✅ Missing `Service Template Blueprint v1.0` — resolved by making it moot: the "Nasljeđuje" line in all six mini blueprints is struck through with an explanatory note rather than creating the missing template, since these are no longer standalone services needing a shared service-page template.
7. ✅ Created `docs/04 - Copywriting/Cutting-Service-Blueprint-v1.0.md` — the primary service never had its own page copy before (only a passing mention); full blueprint written matching the existing set's tone/structure.
8. ✅ Filled in the previously-empty `docs/04 - Copywriting/README.md` with an index of the whole set and current pivot status.
9. ✅ This file's Project Summary updated to the two-service description.
10. ✅ **2026-09-03 consistency re-check (owner explicitly asked "nema li kontradikcija?" — good instinct, there were):** found and fixed five leftover inconsistencies the first pass missed: (a) `README.md` §5/§7 still had two inline links to the pre-rename `docs/04-visual-experience.md`, plus one more in this file's own Technical Decisions (`docs/06-development-rules.md` → should be `docs/07-development-rules.md`); (b) `README.md`'s own new Copywriting-folder note claimed content "still needs revision" when it had already been revised; (c) `CHANGELOG.md`'s `[1.0.2]` "Known gap" list still listed the Service Template Blueprint and empty Copywriting README issues as open when both were already resolved; (d) — the significant one — `Home-Blueprint-v1.0-Full.md` §7 HERO (H1/Lead) still led with "Namještaj po mjeri..." as the primary identity, directly contradicting §8 USLUGE right below it which had already been revised to the two-service framing in the same pass; (e) seven cross-references (six mini blueprints + `Home-Blueprint`, and briefly this file too) pointed to a file literally named `Services-Landing-Blueprint-v1.1.md`, which doesn't exist — the actual file kept its original filename (`-v1.0.md`) and only the version number inside its header changed to 1.1, same convention as `Home-Blueprint-v1.0-Full.md` itself. Fixed all five; added a `[2.0.0]` CHANGELOG entry for the pivot itself (MAJOR per `README.md` §15 semver rule — this was missing entirely) and bumped `README.md`'s version header from 1.0.0 to 2.0.0 to match. Also closed the "not yet decided" room-category URL question below (owner said "ti odluči" in conversation, decision was never written back into the docs) — resolved as plain cards, no anchor sections/sub-URLs, in `Services-Landing-Blueprint-v1.0.md` (interno v1.1) §12.

**Lesson for future sessions:** a cross-file rename/version-bump pass needs a full-repo grep for the OLD name/number afterward, not just fixing the file being edited — this same class of bug (stale filename reference) was introduced twice in one day, including once by this note itself on first draft.

11. ✅ **2026-09-03 full repo re-audit** (owner asked to check "cijeli repo... da li ima kontradiktornosti u pravilima, linkovima, da sve bude u jednom tonu"), done without further owner input as instructed. Read every `.md` file in the repo again end-to-end (root, `/docs`, `/docs/04 - Copywriting`, `/design-system`) and cross-checked every markdown link. Found and fixed six issues:
    - **`00 - Communication System.md` was never revised for the pivot** — the most significant finding. It's the doc every other copywriting file defers to on tone ("ako postoji nedoumica... ovaj dokument ima prednost"), but §6 "Šta prodajemo" only described namještaj-po-mjeri, §7 "Redoslijed razmišljanja" ended every chain in "Namještaj", and §13 "Emocije" had no uslužno-rezanje example. Bumped to v1.1: added a parallel uslužno-rezanje framing to all three sections (reusing phrasing already approved in `Cutting-Service-Blueprint-v1.0.md` for consistency), left the namještaj-po-mjeri content untouched.
    - **No process description for uslužno rezanje** — the single shared "Proces saradnje" page (`Process-Blueprint-v1.0.md`) only had the six-step namještaj-po-mjeri process (ends in Montaža), which doesn't fit the shorter cutting-service flow (no montaža — see `Cutting-Service-Blueprint-v1.0.md` §11). Bumped to v1.1: renamed §10 to clarify it's namještaj-po-mjeri-specific, added §10a with the three-step cutting-service flow, and left a note that the page needs to visually present both tracks (tab/switch/two sections — implementation decision, added to that file's TODO, not decided here).
    - `Home-Blueprint-v1.0-Full.md` §5 Globalna pravila ("Prvo čovjek. Zatim prostor. Tek onda namještaj.") had the same furniture-only assumption, right below a §8 USLUGE that was already two-service framed. Reworded to "Zatim potreba — rezanje ili namještaj po mjeri. Tek onda rješenje."
    - `REPOSITORY_STRUCTURE.md` still said the copywriting folder "needs revision against the current service list" — stale since the `[2.0.0]` revision; fixed.
    - `README.md` had a literal duplicated "Typography" section (pasted twice in a row) and one absolute GitHub URL where every other link in the file is relative — both fixed.
    - `CHANGELOG.md` listed `[1.0.2]` above `[2.0.0]` even though 2.0.0 is the higher version — inverted the newest-first convention from §15. Reordered, and added a new `[2.0.1]` PATCH entry documenting this whole pass.
    - All fixes are recorded in `docs/04 - Copywriting/README.md`'s status section and in `CHANGELOG.md [2.0.1]`. Nothing here changed a locked decision — all six were consistency/completeness fixes within the already-approved 2.0.0 pivot.

Process page dual-track visual decision (resolved 2026-09-03, was the one open TODO from item 11 above): owner asked to clarify the three options (tabs/toggle, two static stacked sections, or static sections + short in-page nav) and picked the third — static sections (uslužno rezanje first, prati poslovni prioritet, then namještaj po mjeri's six steps), plus a short "Rezanje →" / "Namještaj po mjeri →" nav under the Hero that scrolls to the right section. No tabs, no JS dependency — same static approach as `Services-Landing-Blueprint-v1.0.md`. **Provisional pending Neša's final sign-off** — owner expects agreement but it isn't locked yet. Written into `Process-Blueprint-v1.0.md` §7/§10a/TODO/Changelog.

Room-category URL decision (resolved 2026-09-03, was previously open): categories under "Izrada namještaja po mjeri" stay as **plain cards, no dedicated URLs/anchor sections** — simplest option, matches the two-service IA depth, not a permanent lock (mini blueprint content stays available for Projekti filters or deeper sections later if needed).

The brandbook is now built (see the HOLD note above and Session Notes) — these items are no longer blocked on that specifically, but the hold itself is still in effect until the owner explicitly says to resume page/component/layout work:

1. ~~Once ChatGPT copywriting work is finished, build a dedicated Claude Code skill from it (voice, structure, content rules).~~ — done 2026-09-03: `.claude/skills/ivertim-copywriting/SKILL.md`. Operational summary of `docs/04 - Copywriting/` — voice rules, CTA system, banned phrasing, per-page final-copy file map, pattern for writing new content (blog/savjeti, microcopy) not yet covered by a blueprint, and a checklist for reviewing incoming copy from the owner/ChatGPT. Explicitly defers to `00 - Communication System.md` as source of truth and explicitly does not apply the `balkan-direct-response-master` register (see below) to site content.
2. ✅ Usluge content added as two `src/content/usluge/*.md` entries (uslužno rezanje = primary/order 1, namještaj po mjeri = secondary/order 2, the six room categories as sections within the second entry, not separate URLs). Home, Usluge landing, and Proces saradnje built out with real copy and section components — done 2026-09-03, see Session Notes. **Still open:** Projekti and Blog/Savjeti collections are empty — populate only when the owner provides real case studies/articles, never fabricate entries to fill them.
3. ✅ Kontakt page intro + contact info block + FAQ + LocalBusiness JSON-LD structured data — done 2026-09-03 using the confirmed business details (see Resolved above). Structured data validated as parseable JSON in-browser.
4. ✅ Visually validate the fluid typography clamp() values and color contrast in-browser — done 2026-09-04 with real copy in place; found and fixed a real line-height/fluid-size mismatch and a `--text-tertiary` contrast failure, see Known Issues and Session Notes. The clamp() min/max endpoints themselves are still Claude Code's own choice, not owner-confirmed — worth a fresh look once real photography exists and the overall visual pass happens, but the scaling mechanics are now correct rather than just the endpoints.
5. ✅ Mobile navigation pattern built — done 2026-09-03: `Header.astro` now uses a native `<details>/<summary>` disclosure (zero JS, works with JS disabled, keyboard-accessible by default) instead of the old wrapping-links fallback. Verified working via direct DOM check (the browser tool's synthetic click was flaky this session, but the underlying toggle mechanism and rendered markup are both confirmed correct).
6. ✅ Real logo wired into `Header.astro` (horizontal SVG), `Footer.astro` (mark), and the favicon (`public/favicon.svg` now the approved mark, replacing Astro's default rocket icon) — done 2026-09-03. Still open: a dedicated favicon variant, app/social icon, and monochrome variants haven't been sent by the owner — using the general-purpose mark SVG as a stand-in for the favicon slot is a reasonable interim (not a new derivative asset, just reusing the existing approved file), not a final favicon design.
7. ✅ **Write real FAQ answers** — done 2026-09-04 (next session after the one that flagged it). See Current Task for detail.
8. **Visual design iteration pass** — blocked on owner input (their ideas + inspiration screenshots), explicitly deferred to next session. See Current Task for the full context (visual bar, Signature Shape repositioning). Do not start guessing at a new visual direction without that input.

Not blocked, can happen independently whenever convenient:
- ~~Set up real `RESEND_API_KEY` / `CONTACT_EMAIL_TO` / `CONTACT_EMAIL_FROM`~~ — done 2026-09-03, see Technical Decisions / Known Issues for the real values and the FROM-address caveat.
- Decide on a Vercel (or other) project connection when ready to deploy — owner plans to import the GitHub repo into Vercel once local commits are pushed; not done yet, waiting on owner to say when to push.

**Balkan Direct Response Master Skill (2026-09-03):** owner built a general-purpose direct-response/ads/funnel copywriting skill (Hormozi strategy + Gajić local execution, Meta ads/email/landing/sales-scripts) separately with GPT and asked to bring it into Ivertim. Flagged a real tone conflict — it's a hook/urgency/conversion-pressure framework, while `00 - Communication System.md` is explicitly calm/advisory/non-salesy with no false urgency and one fixed CTA. Owner decided to keep them separate: installed as its own personal Claude Code skill at `~/.claude/skills/balkan-direct-response-master/SKILL.md` (available across projects, invoked on demand), **not** merged into Ivertim's docs or voice. Ivertim's own copywriting (the eventual skill from `docs/04 - Copywriting/`, per item 1 above) stays the default/primary voice for the site; Balkan Direct Response is for future paid ads/email/sales-script work only, once that infrastructure exists — none of it exists yet.

## Open Questions

- Dark mode: token naming is dark-mode-ready but dark mode itself is out of scope for v1 launch.
- "Razgovarajmo" is explicitly labeled a *working name* for the primary CTA in `README.md`/`docs/03-web-experience.md` — not necessarily final copy, flagged so it isn't accidentally treated as locked.
- ~~No Vercel project / deployment target is connected yet~~ — resolved 2026-09-03: `ivertim` project live on Vercel (`webkeyzlabs-projects` team), auto-deploys on push to `main`. **Update 2026-09-04:** the owner already bought `ivertim.com` (2026-09-03, via Namecheap) and verified it on Resend for email — but it is **not yet connected as the Vercel deployment's own domain**; the site is still served at `ivertim.vercel.app`, and `astro.config.mjs`'s `site` value still points there. Connecting the domain in Vercel + updating `site` is a small remaining step whenever the owner wants to do it.
- **Signature Shape placement (2026-09-04, open):** owner said the current placements (Home hero background corner, one service-card corner, mobile menu icon) are not "na pravim mjestima" (in the right places) and need "drugačiju poziciju" (a different position) — no specifics given, explicitly deferred to next session. Ask before touching Signature Shape placement again.
- **Instagram content planning (2026-09-04, future phase):** owner's stated sequence is finish the site to their visual bar first, then move to planning Instagram content. Not started, not urgent yet — just don't be surprised when it comes up.
- **Google Business Profile (2026-09-04, future phase):** owner will set this up themselves; once it exists, expect a request to help refine/optimize it (keep NAP — name/address/phone — consistent with the site's `LocalBusiness` structured data in `kontakt.astro`). Nothing to prepare now.

**Pending owner/ChatGPT input (not blocking, just not decided yet):**
- Exact final list of Usluge (service) pages — owner will confirm exact list from the business owner.
- ~~Phone number still missing~~ — resolved 2026-09-03: +387 65 748 943 (Nenad's number). All Business contact details (below) are now confirmed.
- ~~Real domain/email for `CONTACT_EMAIL_FROM`~~ — resolved 2026-09-04: `ivertim.com` verified on Resend, local `.env` now uses `kontakt@ivertim.com`. See Known Issues for the one remaining step (Vercel's production env var still needs the same change).
- Legal/compliance content (privacy policy, cookies, impressum) — will be produced by the owner or ChatGPT, not authored here by default.
- ~~Contact-Blueprint §10's "Showroom" assumption~~ — resolved 2026-09-03: no showroom exists. Svale bb is a factory with new cutting machines (long-standing) plus offices in front; a future IKEA-style design showroom was considered pending office renovation, but the owner is deliberately de-emphasizing that consumer-design-service model in favor of the drvni centar focus ("taj model povlačimo lagano i neprimjetno"). Removed "Showroom" from `Contact-Blueprint-v1.0.md` §10 entirely rather than softening it — it doesn't currently exist, so keeping any version of it as a listed contact channel would be inventing a value. If a showroom is built later, add it back as a secondary mention, never a primary CTA.

**Resolved:**
- **Service pivot (2026-09-02, owner spoke with business owner Neša):** the two services to be marketed going forward are (1) uslužno rezanje, kantovanje i bušenje pločastih materijala, and (2) izrada pločastog namještaja po mjeri. Production is reorienting more toward the "drvni centar" (wood/board cutting service center) model than toward made-to-measure furniture. "Kompletno opremanje stanova" will not be listed as a separate marketed service — the owner already has a referral network that keeps that domain filled without marketing. This is an explicit new owner decision and, per the README §1 authority hierarchy, supersedes the current text of `docs/00-strategy.md`, `README.md` §2, `docs/08-marketing.md`, and the `docs/04 - Copywriting/` set — see Next Task for the reconciliation plan.
- Language: Serbian, ijekavica, single-language for now. Bilingual (Serbian + English only) is a possible future addition, explicitly out of scope for v1.
- Technical stack: Astro + TypeScript, approved by owner and ChatGPT — see Current Architecture / Technical Decisions.
- Content authoring path: Astro Content Collections (markdown/MDX in-repo), no CMS.
- **Pivot revision + §10a Process-page decision confirmed (2026-09-03):** owner confirmed on Neša's behalf ("praktičan čovjek, reći će mi da napravim da vidi") — both the 2026-09-02/03 service-pivot documentation revision and the Process-page dual-track visual decision (static stacked sections, rezanje first, short in-Hero nav) are no longer provisional. Updated in `Process-Blueprint-v1.0.md`.
- **Business contact details (2026-09-03, fully confirmed):** legal entity is "Zajednička zanatska radnja IVER TIM Nenad Crnogorac Bojan Jeftić s.p Prijedor" (per Rješenje o osnivanju) — owner: "Ivertim je dovoljno" for public-facing display, so use the legal name only where formally required (e.g. a `legalName` field in structured data, footer fine print), never as the primary displayed brand name. Phone: +387 65 748 943 (Nenad's, not Bojan's). Email: `namjestajivertim@gmail.com` (temporary, no custom domain — see the `CONTACT_EMAIL_FROM` note above). Address: Svale bb, 79101 Prijedor — a factory with the new cutting machines plus offices in front, not a retail location. Radno vrijeme: radnim danima 7–16h, vikendom zatvoreno. No showroom — see the resolved Pending item above for why it's deliberately absent from Contact-Blueprint.

## Technical Decisions

**Approved 2026-07-11 (owner + ChatGPT):**
- Framework: **Astro**, TypeScript, static-first output with `@astrojs/vercel` adapter. Rationale: content/brochure-style site, SEO and minimal shipped JS are priorities per `docs/07-development-rules.md`; Astro ships zero JS by default (islands only where needed).
- Content: **Astro Content Collections** (markdown/MDX in-repo), no CMS, no database.
- Styling/tokens: **CSS custom properties** generated directly from `/design-system`, no Tailwind (kept optional, not added — avoids a second token system).
- Images: `astro:assets` (not yet used since no real images exist yet).
- Animation: plain CSS transitions using the motion tokens, no animation library.
- Contact form: Astro server endpoint (`/api/contact`, `prerender: false`) + Resend, isolated behind `src/lib/email.ts` so the provider can be swapped. Server-side validation + honeypot spam protection implemented; no third-party anti-spam vendor (e.g. Turnstile) added — would need proposing separately if spam becomes a real problem.
- Deployment target: Vercel (previously "considered" in docs, now the actual adapter installed). Not yet connected to a live Vercel project.
- Testing: not set up yet — plan remains Playwright for a couple of critical paths (nav, contact form) once there's real content to test against; no unit test framework unless isolated logic emerges that warrants it.
- i18n: structure kept ready for a future `/en/` addition (Astro has built-in i18n routing) but not implemented — English routes are out of scope for v1.

**Guardrails (apply regardless of stack):**
- Mobile-first, semantic HTML, progressive enhancement, accessible components.
- All design values must come from design-system tokens — no hardcoded HEX/px where a token exists.
- No CMS, auth, database, analytics/tracking, major UI library, or new runtime/deploy platform without proposing first.

Locked product/brand/design decisions (must not change without explicit owner approval):

- **IA:** Home, Usluge (per-service URLs), Projekti (per-project URLs), Proces saradnje, Blog/Savjeti, Kontakt. Single nav CTA, working name "Razgovarajmo".
- **Color:** Light mode is primary/only mode for v1. Brand accent = Srednji orah (`accent-500 #805A3C`, primary action `accent-600 #6E4D33` / hover `accent-700 #5C402A` / active `accent-800 #493221`). Green reserved strictly for functional success states, never brand accent. No second decorative accent family.
- **Visual language:** clarity > novelty, whitespace > border > shadow, one primary CTA per context. Banned: glassmorphism, neumorphism, neon glow, colored shadows, heavy gradients, default carousels, decorative-only animation.
- **Logo:** rounded-footprint mark, single controlled stroke, no slogan alongside primary logo in phase one.
- **Photography:** real process/material/measurement shots only; no generic stock, no over-retouched or AI-perfect scenes.
- **Content mix (blog):** 50% educational / 30% sales-adjacent / 20% other.
- **Typography:** Instrument Sans Variable is the official typeface (single family, variable font, fallback `Instrument Sans, system-ui, sans-serif`). Hierarchy comes from size/weight/line-height/letter-spacing only — never a second font family without approval.

## Known Issues

- `npm audit`: back down to the original 3 high-severity advisories, all transitive (`path-to-regexp` ReDoS via `@vercel/routing-utils` inside `@astrojs/vercel`). A 2026-09-04 `npm audit fix` (non-force) cleared 9 newly-appeared advisories that came in with `@astrojs/sitemap`'s own dependency tree (postcss, js-yaml, tar, nanoid, fast-uri, brace-expansion, plus one astro core one) — left only this original cluster, which still needs `--force` (a breaking adapter downgrade) and is still deliberately deferred until the adapter ships a clean fix.
- ~~Fluid typography `clamp()` values for display/H1–H3 and `--section-gap` use mobile minimums Claude Code invented~~ — **partially validated 2026-09-04**: found and fixed a real bug where the six display-lg/md/sm + H1-H3 `-leading` tokens were fixed px (correct only at the desktop max of the fluid clamp()), so line-height didn't scale down with the shrinking font-size — headings got visibly loose at small viewports (measured 1.88 leading ratio vs. the intended ~1.14 at the mobile minimum). Converted those six tokens to unitless ratios in `tokens.css` (renders pixel-identical to before at the desktop max, scales correctly below it) — see the file's header comment for the full explanation. Verified via computed-style checks at 375px and 1536px, and no horizontal overflow on any real-copy page at 375px. The clamp() min/max values themselves are still Claude Code's own reasonable choice, not owner-confirmed, and still worth a real visual look once photography exists — but the scaling mechanism itself is now correct, not just the endpoints.
- ~~`Header.astro` mobile nav just wraps rather than using a real mobile menu pattern~~ — resolved 2026-09-03, see Next Task item 5.
- ~~Contact form cannot currently deliver to the real business inbox~~ — **fixed 2026-09-04, one manual step left (production env var).** Confirmed broken earlier the same session (Resend's sandbox address can only send to the Resend account's own signup email, never to `CONTACT_EMAIL_TO`), and flagged as needing an owner decision rather than a unilateral fix. Owner then shared screenshots showing `ivertim.com` was already purchased and verified on Resend (DNS + domain verified, ~22h before this was flagged) — a domain-scoped API key ("Ivertim Resend") already existed too, and turned out to be the exact same key already in local `.env` (matching `re_GRqUFPLH...` token prefix), so no key rotation was needed. Updated local `.env`'s `CONTACT_EMAIL_FROM` from `onboarding@resend.dev` to `kontakt@ivertim.com` and re-tested end-to-end (with the owner's explicit go-ahead for the real send) — `status=success`, no error in the server log. **Still open: the same `CONTACT_EMAIL_FROM` change needs to be made in Vercel's project environment variables (Settings → Environment Variables, Production) and redeployed** — local `.env` and Vercel's env vars are separate, and no Vercel MCP tool here can write env vars, so this one step needs the owner (or a `vercel env` CLI command) to apply directly. Until that happens, production is still sending from the old sandbox address and is still broken for real visitors even though local dev now works.
- `ivertim.com` is registered (via Namecheap) and verified on Resend as of 2026-09-03 — **not yet connected as the site's own domain in Vercel**, still deployed at `ivertim.vercel.app`. Previously "deliberately deferred" per the owner (see Open Questions) — that note is now stale for the *domain purchase* itself (already done), though connecting it to the Vercel deployment and updating `astro.config.mjs`'s `site` value is still a separate, not-yet-done step.
- ~~Two old logo PNGs still in `public/images/`~~ — removed 2026-09-03, owner confirmed.
- **`--text-tertiary` (neutral-500, `#958A7E`) fails WCAG AA contrast at normal text sizes — found and fixed 2026-09-04.** Measured ~3.1–3.4:1 against `bg-primary`/`surface-1` (needs 4.5:1 for text under ~18px/24px); it was used at 12–14px in `Footer.astro` (overline label + legal text), `index.astro` (`.reassurance`), `blog/index.astro` (`.empty`, `.post-meta`), `proces-saradnje.astro` (`.step-label`), and `projekti/index.astro` (`.empty`) — seven instances across six files, all genuine failures, not edge cases. Swapped all seven to `--text-secondary` (7.4–8:1, comfortably AA/AAA) — a token swap, not a change to any locked color value in `tokens.css`/`design-system/colors.md`, since `text-tertiary` itself isn't flagged with a size restriction in the locked spec (colors.md has no usage note either way; the mismatch was purely in how implementation had been using it). `text-tertiary` remains a valid design-system token — just don't reach for it below ~18px/24px normal weight without checking contrast first.
- The Claude Browser preview tool's synthetic `computer` click action was flaky against `Header.astro`'s mobile menu toggle this session (intermittent 30s timeouts and one stale-screenshot render on `proces-saradnje`) — verified via direct DOM/computed-style inspection that the actual page markup, CSS and native `<details>` toggle all work correctly; treated as a tool-session quirk, not a site bug. Recurred 2026-09-04 against the contact form's submit button (negative off-screen coordinates after a hot-reload) — worked around by POSTing directly to `/api/contact` with matching Origin/Referer headers instead of fighting the UI. Still not urgent, but recurring enough now that it might be worth a closer look if it keeps happening.

## Session Notes

- **2026-09-04 (contact form fixed — domain was already bought):** After the contact-form finding was reported, owner asked what was actually needed, got the two options (verify a domain vs. temporary redirect) plus a recommendation, then shared two screenshots instead of choosing: `ivertim.com` was already registered via Namecheap and verified on Resend ~22h earlier (2026-09-03), with a domain-scoped API key ("Ivertim Resend") already created — this had already been done but not communicated or reflected in `.env`. Confirmed the existing local `RESEND_API_KEY` already matched that key's token prefix (no rotation needed), so the only change needed was `CONTACT_EMAIL_FROM`: `onboarding@resend.dev` → `kontakt@ivertim.com`. Asked the owner for explicit permission before sending a real test email (one attempt was blocked by the auto-mode classifier as an outbound-send action; asked directly instead of working around it) — owner said "možeš" — then sent one real test submission end-to-end: `status=success`, no error in the server log, confirming Resend accepted it. **One step still open, needs the owner or a `vercel env` command**: Vercel's production environment variables need the same `CONTACT_EMAIL_FROM` change — no Vercel MCP tool available here can write env vars, and local `.env` doesn't propagate to Vercel. Also noted: the domain isn't connected as the Vercel deployment's own domain yet (still `ivertim.vercel.app`) — separate step from the email fix, whenever the owner wants it.
- **2026-09-04 (real blog articles + housekeeping pass, same session continued):** Owner asked "bez slika, šta još možemo" (without images, what else can we do). Offered four candidates (blog articles, contact-form end-to-end test, typography/contrast validation, technical housekeeping); owner picked all four, in that order. Results:
  1. **Wrote 3 real Blog/Savjeti articles** (not just structure) — a judgment call flagged and confirmed with the owner before writing, since prior notes treated "real articles" as owner-supplied. Reasoning: educational how-to content doesn't carry the same fabrication risk as invented case studies of real completed work (which Projekti would be) — it's general, verifiable craft knowledge, matches the locked 50/30/20 content mix, and the blueprint's own worked kitchen-planning example was written specifically to be publishable. Published: `kako-planirati-kuhinju.md` (the blueprint's own approved example, used verbatim), `priprema-za-uslusno-rezanje.md` (new — what to prepare before ordering cutting/kantovanje/bušenje, matches `docs/08-marketing.md`'s #1 topic priority post-pivot), `namjestaj-po-mjeri-ili-gotova-rjesenja.md` (new — one of the blueprint's own suggested titles). All three kept generic on anything requiring a confirmed business fact (no invented tolerances/prices/timelines), all `featured: true`, all `category: edukativno`.
  2. **Tested the contact form end-to-end with the real Resend key for the first time — found it's actually broken.** See Known Issues for the full finding: Resend's sandbox address can only deliver to the Resend account's own signup email, never to `CONTACT_EMAIL_TO`, in dev or production. Added error logging so future failures are visible in Vercel logs instead of silent. This needs an owner decision (verify a domain, or temporarily redirect where inquiries land) — flagged prominently, not fixed unilaterally.
  3. **Typography/contrast validation** (Next Task item 4, now partially closed): found and fixed two real bugs — (a) fluid heading line-height was fixed px against a shrinking clamp() font-size, causing visibly loose headings on mobile (fixed by converting six `-leading` tokens to unitless ratios, pixel-identical at desktop, correct below it); (b) `--text-tertiary` fails WCAG AA at the small sizes it was actually used at in seven spots across six files (swapped to `--text-secondary`). Neither touched a locked color/size value — both were implementation-level misuses of tokens whose values themselves stayed within `design-system/colors.md` and `typography.md`.
  4. **Technical housekeeping**: added `@astrojs/sitemap` + `site` config (Vercel's stable alias, found via the Vercel MCP tools — no custom domain connected yet, update when one is), `public/robots.txt`, a real `src/pages/404.astro`, and canonical/OG/Twitter meta tags in `BaseLayout.astro`. Also ran `npm audit fix` (non-force) — cleared 9 advisories that arrived with `@astrojs/sitemap`'s own deps, leaving only the original pre-existing 3.

  `npm run build` verified clean throughout. Not yet committed at the time of this note — everything above is one pending commit (or a few) once reviewed.
- **2026-09-04 (Blog/Savjeti structure):** Owner asked, unprompted by any blocked item, to build the Blog/Savjeti page structure — landing page layout/functionality, then the individual article template — explicitly "ne treba pretjerivati" (don't overdo it). This is structural/component work, not the actual articles (still correctly withheld per the standing no-fabrication rule). Read `Advice-Blueprint-v1.0.md` and `Advice-Template-Blueprint-v1.0.md` (both Approved v1.0, full final copy) and implemented their structure and copy verbatim. Added `topic` (reader-facing category) and `featured` fields to the `blog` schema, and a shared `src/lib/blog.ts` for topic labels + an auto-computed reading-time estimate (no manual field to maintain). Rebuilt `blog/index.astro` (Hero → Zašto dijelimo znanje → Istaknuti savjeti cards → Kategorije → Završni CTA, matching the visual pattern already established on Home/Usluge — `.section`/`data-reveal`/card styles) and `blog/[slug].astro` (meta row with reading time + topic, hero image slot, `.prose` content, then a hardcoded closing-CTA block since that copy is identical on every article per the blueprint, not per-post content). QA'd both pages end-to-end with a temporary test article built from the blueprint's own worked kitchen-planning example (verbatim, since it's already approved copy) — confirmed cards, reading time, topic label, numbered lists, and the closing CTA all render correctly — then deleted the test file before committing, so no fabricated content ships. `npm run build` clean both with and without the test post. See Current Architecture for the technical breakdown, including two things deliberately left out for now (category filtering, an archive beyond 4 featured cards) as real but not-yet-relevant gaps. Not yet committed at the time of this note.
- **2026-09-04 (FAQ answers — new session, no owner input yet on the visual-direction pass):** Owner said "pogledaj gdje smo stigli pa nastavi dalje po svom izboru" (look at where we got to, then continue on your own choice). Read the handoff and picked the one item explicitly flagged as unblocked and not needing owner visual input: writing real FAQ answers (Next Task item 7). Used the `ivertim-copywriting` skill; kept every answer generic wherever a real business fact (materials, order quantities, lead times) is still unconfirmed, per `Cutting-Service-Blueprint-v1.0.md` §15 TODO. Details in Current Task. Did not touch Signature Shape placement or start any visual-direction work — both are still waiting on the owner's own ideas/inspiration screenshots as recorded below. Working tree not yet committed — next step is a commit + push, then wait for owner's visual input or further instruction.
- **2026-09-04 (session end — owner review + roadmap):** Owner reviewed the motion/Signature Shape pass, reaction still "nije to to" — structure is fine but visual craft needs another pass, and asked to end the session here. Full detail is in **Current Task** (read it first next session) — short version: (1) owner will drive the next visual iteration with their own ideas and inspiration screenshots, not just docs-compliance from this end; (2) explicit visual bar set — readable AND striking, "invests in visual feel" not just "serious company"; (3) Signature Shape placement flagged as wrong, specifics deferred; (4) found and recorded a real content gap — FAQ questions exist in two `usluge` entries with zero answers, only Kontakt has real Q&A; (5) roadmap after the site: Instagram content planning, and owner will independently set up a Google Business Profile for later refinement together. Recorded as Next Task items 7–8 and several Open Questions entries. Nothing built this exchange — pure documentation/handoff.
- **2026-09-04 (motion + Signature Shape pass):** Owner reviewed the real-content build and felt it was "šturo" (sparse) — asked specifically for light animations and Signature Shape usage, wondering if the design-system rules were too strict. They weren't: both were already permitted (`design-system/motion.md`; `docs/09 - Signature System.md` §CTA Elements explicitly sanctions replacing "generic arrows or decorative icons" with the shape) — just not implemented yet in the first content pass. Added: a shared scroll-reveal mechanism (`BaseLayout.astro` + `global.css`, IntersectionObserver on `[data-reveal]`, respects reduced-motion) applied to major sections across Home/Usluge/Proces/Kontakt; replaced `Header.astro`'s mobile hamburger icon with the Signature Shape mark (owner's specific suggestion) with a smooth rotate/fade cross-transition to the close state; one Signature Shape corner accent on the primary service card only on Home + Usluge (never all cards, per docs/09 §Cards), plus a low-opacity background shape behind the Home hero (§Section Backgrounds). `npm run build` verified, committed as `2a19749`, pushed. Photography remains the bigger lever for the "sparse" feeling and still doesn't exist — flagged to the owner as the next real priority once available.

- **2026-09-03 (cleanup + owner feedback on the build):** Owner reviewed the real-content build. Confirmed removal of the superseded PNG logos (`logo-horizontal.png`, `logo-mark.png` — deleted, `docs/02-logo-identity.md`/`REPOSITORY_STRUCTURE.md` updated) and confirmed the interim favicon choice (`logo-mark.svg`, no change needed). Qualitative feedback: structure is decent ("prilično ok") but the site reads as sparse/bare ("šturo") without real photography — and explicitly noted this isn't only about images, it's still far from feeling like a finished real site. Expected and consistent with `docs/05-visual-experience.md` (premium comes from photography + rhythm + whitespace together, not structure alone) — no real photos exist yet. Next real lever for "feels finished" is photography once the owner has it; visual/rhythm polish can also help in the meantime but won't fully substitute for real images.
- **2026-09-03 (first real content build):** Owner confirmed lifting the dev hold via a direct question. Built out Home, Usluge (landing + two content-collection entries), Proces saradnje, and Kontakt with real copy — all verbatim or lightly assembled from `docs/04 - Copywriting/` blueprints, none invented (see the commit message on `f4d3c14` for the full per-file breakdown). Also: replaced `Header.astro`'s wrapping mobile nav with a zero-JS `<details>/<summary>` disclosure pattern; wired the real logo SVGs into `Header.astro`/`Footer.astro`/favicon; added prose spacing CSS to `usluge/[slug].astro` (collection content had no paragraph/heading spacing before); gave Projekti/Blog calmer empty-state copy without fabricating entries. Caught and fixed a genuine typo in `Custom-Blueprint-v1.0.md` ("izražaja dolazi" → "do izražaja dolazi") both in the shipped page copy and the source blueprint. Installed npm dependencies (were never installed on this machine), verified `npm run dev` (no console/server errors) and `npm run build` (succeeds) both work, and did a manual walkthrough of all four pages including a 375px mobile check of the new nav — the browser tool's synthetic clicks were flaky against the mobile toggle specifically (see Known Issues) but direct DOM inspection confirmed the toggle and rendered markup are both correct. Committed as `f4d3c14` and pushed to `main`.
- **2026-09-03 (contact details + Resend):** Owner answered the outstanding "what's next" items directly instead of waiting for the dev hold to lift: confirmed the 2026-09-02/03 pivot revision and the Process-page §10a decision on Neša's behalf; gave full business contact details (legal name, email, address, working hours — phone still missing actual digits); and uploaded a real Resend API key. Wrote `RESEND_API_KEY`/`CONTACT_EMAIL_TO`/`CONTACT_EMAIL_FROM` into local `.env` (verified git-ignored, not `.env.example`, never committed) — `CONTACT_EMAIL_FROM` uses Resend's sandbox address since no domain is verified yet, per owner's explicit choice to defer that. Updated `Process-Blueprint-v1.0.md` (confirmed, not provisional) and `Contact-Blueprint-v1.0.md` TODO (resolved items, flagged the still-missing phone number and an unconfirmed "showroom" framing assumption in §10 given the business is a workshop, not a shop). Recorded everything here since none of this is page-content work yet — the dev hold is still in effect and this data is prepared for whenever it lifts, not implemented into `src/pages/kontakt.astro` yet. Owner also said Vercel import happens after a GitHub push, once we do it — not done yet, waiting on an explicit push instruction. **Follow-up same session:** owner sent the phone number (+387 65 748 943) and explained the showroom question in full — Svale bb is the long-standing factory (now with new cutting machines) plus offices in front; a future IKEA-style design showroom was floated pending office renovation, but the owner is deliberately de-emphasizing that consumer-design-service angle in favor of the drvni centar focus. Removed "Showroom" from `Contact-Blueprint-v1.0.md` §10 entirely (bumped to v1.1) rather than softening it, since it doesn't exist yet and keeping it would be inventing a value — noted it can be added back later as a secondary mention if the owner builds it. All Business contact details are now fully confirmed, no open fields left.
- **2026-09-03 (brandbook):** Owner asked to build the visual-identity brandbook directly using Claude Code's `/design` skill rather than waiting on a separate Claude Design session. Built a 9-slide brandbook (cover, logo system, color, typography, geometry/spacing, Signature Shape, motion, photography/art direction, site application mockups) sourced only from already-locked `design-system/*`, `docs/02`, `docs/05`, `docs/09`, and approved copy from `docs/04 - Copywriting/` — no new decisions invented. A background review pass caught and fixed real errors before showing the owner: an unapproved second font family (JetBrains Mono) used for token labels, invented logo safe-zone/usage rules not in `docs/02-logo-identity.md`, and composed (non-verbatim) card copy that its own footer wrongly claimed was verbatim. After the owner delivered final SVG logo assets same day (see below), updated the brandbook to use them (previously used raster PNG approximations, including for the Signature Shape composition which now uses the real vector). Owner also caught a real design-system violation: most slides used `#FFFFFF` as the page background with `#F7F6F4` for cards — backwards from `colors.md`'s actual bg-primary/surface-1 hierarchy (page should be `#F7F6F4`, cards `#FFFFFF`) — fixed across all 7 light slides. Published as a Claude Artifact: **https://claude.ai/code/artifact/622905e0-ecaa-438c-b856-e6e76a45b87c**. Working source files live in this session's scratchpad only (not in the git repo) — if a future session needs to edit this brandbook, read the artifact back with the Artifact tool rather than looking for local files.
- **2026-09-03 (continued further):** Owner delivered final SVG logo assets (8 files: horizontal lockup, mark, wordmark, Signature Shape — each light + dark/inverse) and asked to add them to the repo. Copied into `public/images/` with clear names (see Current Architecture), updated `docs/02-logo-identity.md` Asset fajlovi section, `README.md` §5, and `REPOSITORY_STRUCTURE.md`'s tree/reading-order to reflect them and what's still missing (vertical lockup SVG, favicon, app icon, monochrome). Left the old superseded PNGs (`logo-horizontal.png`, `logo-mark.png`) in place rather than deleting them unasked — flagged as an open cleanup question. Also caught and removed `logo-horizontal-small.png`, a temp downsampled PNG that had accidentally been created directly in `public/images/` during the earlier brandbook work (should have stayed in scratchpad) — not part of the real asset set, never committed.
- **2026-09-03 (continued):** Owner asked what the next phase should be. Proposed building the copywriting skill (unblocked now that the pivot revision is content-complete, and doesn't touch the dev hold since it's tooling, not page work) — owner agreed. Built `.claude/skills/ivertim-copywriting/SKILL.md`: a compact operational reference (not a duplicate of the full blueprint set) covering voice/tone, the two-audience selling logic, CTA system, banned phrasing, a file map pointing to where final copy already exists per page, a pattern for writing new content the blueprints don't cover yet (blog/savjeti, microcopy), and a checklist for vetting copy that arrives from the owner or ChatGPT. It explicitly defers to `00 - Communication System.md` and explicitly excludes itself from ad/funnel/sales-script work (that's `balkan-direct-response-master`'s job, kept separate per the earlier decision this session). Committed to `main`.
- **2026-07-11:** Initial full documentation read-through; created this `CLAUDE.md`. No implementation work done. Next session should open with a proposed stack/architecture for approval before writing any code.
- **2026-07-11:** Locked Instrument Sans Variable as the official typeface. Updated `design-system/typography.md`, `README.md`, and `CHANGELOG.md` per explicit owner instruction.
- **2026-07-11:** Reorganized this file into the fixed section structure (Project Summary / Current Architecture / Current Progress / Completed / Current Task / Next Task / Open Questions / Technical Decisions / Known Issues / Session Notes) per owner instruction.
- **2026-07-11:** Owner confirmed: documentation phase was done with ChatGPT; Claude Code now owns Development going forward. Confirmed workflow is plan-first, build-second, and that this file must be updated at the end of every meaningful session with what was done, current status, and the next step in order. No implementation work started yet — still in planning/handoff stage.
- **2026-07-11:** Reviewed open questions with owner. Resolved: language is Serbian ijekavica only for now (English possibly later). Clarified as pending owner/ChatGPT input rather than truly open: exact Usluge page list, business contact details, legal/compliance content. Copywriting continues in ChatGPT (multi-day effort); plan confirmed to eventually turn that finished copy into a dedicated Claude Code skill (added to Next Task, not started).
- **2026-07-11:** Proposed Astro + TypeScript stack; owner ran it by ChatGPT, which approved with refinements (plain Markdown by default, hybrid-render contact endpoint with server validation/honeypot/isolated email module, i18n-ready structure, tokens-only-from-design-system). Owner approved everything and granted broad implementation autonomy ("radi kako misliš da je najbolje"), with the site needing to be functional by end of August 2026. Scaffolded the full Astro project end-to-end (see Current Architecture), fixed two real bugs found during verification (deprecated `astro:content` zod re-export → import from `zod` directly; `/kontakt` needed `prerender: false` to read post-submit query params, since it was silently swallowing the status message otherwise), verified dev server + production build + contact endpoint behavior, and made the first local git commit. Next session should pick up once real content/copy/contact details arrive.
- **2026-07-11:** Owner reviewed the scaffold and put an explicit **hold on further page/component/visual development**: technical scaffold stays as built, but no page development, section components, visual layouts, or placeholder copy until Copywriting is fully finished and content for all pages is confirmed. Also reconfirmed the fluid typography `clamp()` values Claude Code chose are explicitly temporary, not a final decision. Nothing further built this session — Next Task items are now blocked pending that content handoff.
- **2026-07-12:** Owner created a GitHub repo and asked Claude Code to push the full project there, add the three real (PNG placeholder) logo asset files owner supplied in `public/images/`, and make sure the repo structure is clear enough for a separate Claude Design session to understand strategy/identity/web experience/tokens/visual principles/scaffold from it. Renamed the logo files to a consistent `logo-horizontal.png` / `logo-vertical.png` / `logo-mark.png` naming, rewrote `REPOSITORY_STRUCTURE.md` to reflect the actual current tree (previously only listed the pre-scaffold docs-only structure), updated `README.md`'s status line and logo section to point at the real assets and at `CLAUDE.md` for current technical status, and added an asset-file pointer section to `docs/02-logo-identity.md`. Pushed everything to `github.com/WebleyZLAb/ivertim`. Owner will now build a brandbook/presentation with Claude Design from this repo (not inventing new identity, just presenting what's already locked) — dev hold stays in effect until both that and Copywriting are done.
- **2026-09-03:** Owner asked to review status (clarifying `ivertim-erp` is a separate, unrelated project not visible from here), then asked for a full repo re-audit for contradictions/broken links/tone consistency, and to fix everything possible without further owner input, keeping the fixes consistent with Ivertim's tone and the new two-service direction. Re-read every `.md` file in the repo and fixed six issues — see Next Task item 11 for the itemized list. The headline finding was that `00 - Communication System.md`, the foundational tone document, had never been touched by the 2026-09-02 pivot revision even though it's supposed to have final say on tone for everything else in the folder; it and `Process-Blueprint-v1.0.md` are now both v1.1. Everything else was documentation hygiene (duplicate section, stale cross-reference, changelog ordering). Owner then asked to clarify the one open implementation-judgment call left from that pass — how the Process page should visually present its two process tracks — and, after hearing the three options, picked static stacked sections (rezanje first) + a short in-Hero nav, provisional pending Neša's sign-off; written into `Process-Blueprint-v1.0.md`. None of this changed a locked decision. Still uncommitted — working tree only.
- **2026-09-02 (new machine/session — repo re-cloned to `C:\claude-projekti\ivertim`):** Owner reported that between 2026-07-12 and now, no further dev/copywriting/brandbook work happened here (owner paused everything), but real changes did land directly on GitHub outside a Claude Code session on 2026-07-22/23: the full `docs/04 - Copywriting/` blueprint set (Communication System + 13 page blueprints, all "Approved") and `docs/09 - Signature System.md` were added, and `docs/04-07` were renumbered to `05-08` to make room. Owner asked for a full documentation audit before resuming anything, suspecting drift from working with ChatGPT across many files. Read every file in the repo (root docs, all of `/docs` including the Copywriting subfolder, all of `/design-system`, `/src`) and found: (1) the entire copywriting set + `docs/00-strategy.md` + `README.md` §2 + `docs/08-marketing.md` describe made-to-measure furniture (per-room categories: kitchens/wardrobes/living rooms/etc.) as the primary service, with service-cutting only mentioned in passing for small workshops; (2) `README.md` §14 and `REPOSITORY_STRUCTURE.md` still linked/listed the pre-rename `04-07` doc filenames and didn't mention the Copywriting folder or Signature System at all; (3) this file (`CLAUDE.md`) was stale, describing the copywriting set as "not yet delivered as files"; (4) `CHANGELOG.md` never recorded the 07-22/23 additions; (5) six mini blueprints (Kitchens/Wardrobes/Living-Room/Hospitality/Business/Custom) declare inheriting a `Service Template Blueprint v1.0` that does not exist anywhere in the repo; (6) `docs/04 - Copywriting/README.md` is an empty file. `design-system/*` and `src/` were checked and found internally consistent / unaffected. Reported findings to owner. Owner then delivered the actual business decision (having just spoken with business owner Neša): going forward the marketed services are (1) uslužno rezanje, kantovanje i bušenje pločastih materijala and (2) izrada pločastog namještaja po mjeri — production reorienting more toward the "drvni centar" model; "kompletno opremanje stanova" drops as a separately marketed service (saturated referral network already covers it) — recorded under Open Questions/Resolved. Fixed the structural issues immediately (approved by owner): corrected `README.md` §14 links and added the missing Copywriting/Signature System entries, fixed `REPOSITORY_STRUCTURE.md`'s tree and reading order, added a backfilled `CHANGELOG.md` entry (`[1.0.2]`) for the 07-22/23 additions, and updated this file's stale claims. Owner then answered the one remaining open question — what happens to the six per-room mini blueprints — with "sav namještaj će se raditi... ti odluči kako je najbolje", i.e. all room types stay in scope but the AI should decide the site-structure implication. Decided: keep them as examples/cards under the "Izrada namještaja po mjeri" service rather than standalone services, since capacity constraints mean namještaj po mjeru can't always be accepted while cutting-service demand is high — a genuine second-tier service, not six parallel ones. Executed the full content-level reconciliation this same session: rewrote `docs/00-strategy.md` §Poslovni fokus, `README.md` §2, and `docs/08-marketing.md` §Glavne teme to the two-service taxonomy; revised `docs/04 - Copywriting/Services-Landing-Blueprint-v1.0.md` to v1.1 (two services instead of room-category grouping) and `Home-Blueprint-v1.0-Full.md`'s USLUGE section; annotated all six mini blueprints with their new role; wrote the previously-missing `Cutting-Service-Blueprint-v1.0.md` for the primary service (had no page copy before); filled in the empty `docs/04 - Copywriting/README.md` with a set index. See Next Task for the itemized list. None of this has been committed to git yet — working tree only, pending owner review.
